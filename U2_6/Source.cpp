#include <iostream>
#include <math.h>
#include <string>
using namespace std;

void main() {
	/*cout << "Ingrese la cantidad de congresistas en la sesión." << endl;
	int num;
	cin >> num;
	int afv = 0;
	int ecn = 0;
	int abs = 0;
	int opn;
	int i = 1;
	for (i; i <= num; i++) {
		cout << "Ingrese su opinión sobre la encuesta actual (1, 2, 3):" << endl;
		cin >> opn;
		if (opn == 1) {
			afv++;
		}
		else if (opn == 2) {
			ecn++;
		}
		else if (opn == 3) {
			abs++;
		}
		else { 
			cout << "Opinión incorrecta." << endl; 
		}
		if (i == num) {
			cout << "A favor: " << afv << endl;
			cout << "En contra: " << ecn << endl;
			cout << "Abastenciones: " << abs << endl;
		}
	}*/
	
	
}